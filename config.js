const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "1qaz0plm",
      database: "rentas",
    },
    listPerPage: 10,
  };
  module.exports = config;